# Tests must not commit a bootstrap user outside @Transactional.
magyen.security.bootstrap.enabled=false

# Keep Surefire output small; production still uses show-sql from application.yml.
spring.jpa.show-sql=false
